name: pixa_fetcher_s3
description: Official Pixa S3 fetcher plugin descriptor package.
version: 0.1.0-dev.1
repository: https://github.com/fluttercandies/pixa
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  pixa:
    path: ../pixa

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

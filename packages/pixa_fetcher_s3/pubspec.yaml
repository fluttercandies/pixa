name: pixa_fetcher_s3
description: Official Pixa AWS SigV4 image fetcher with S3-compatible endpoint support.
version: 1.0.0
repository: https://github.com/fluttercandies/pixa
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  pixa: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=5.0.0 <7.0.0"

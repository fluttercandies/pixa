# Changelog

## 1.0.0

- Initial public release of the official Pixa S3 fetcher descriptor package.
- Adds `PixaS3.request`, `PixaS3.provider`, `PixaS3.image`, and
  `PixaS3FetcherPlugin` for routing S3 and S3-compatible object requests
  through the Pixa runtime pipeline.

name: pixa
description: Production-oriented Flutter image loading, caching, and pipeline primitives.
version: 0.1.0-dev.1
repository: https://github.com/fluttercandies/pixa
publish_to: none
resolution: workspace

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  code_assets: ^1.2.1
  ffi: ^2.2.0
  flutter:
    sdk: flutter
  hooks: ^2.0.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets: []
